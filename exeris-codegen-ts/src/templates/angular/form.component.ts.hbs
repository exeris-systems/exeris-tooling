/**
 * {{entityName}} Form Component
 * Generated by @exeris/codegen-ts v0.2.0
 * Angular 21+ with Signals and standalone components
 * DO NOT EDIT - This file is auto-generated
 */

import {
  Component,
  ChangeDetectionStrategy,
  input,
  output,
  inject,
  signal,
  computed,
  effect,
} from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule, ReactiveFormsModule, FormBuilder, Validators } from '@angular/forms';
import { {{entityName}}, {{entityName}}Create, {{entityName}}Update, {{entityName}}Service } from '../services/{{kebabCase entityName}}.service';

@Component({
  selector: 'app-{{kebabCase entityName}}-form',
  standalone: true,
  imports: [CommonModule, FormsModule, ReactiveFormsModule],
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <form [formGroup]="form" (ngSubmit)="onSubmit()" class="space-y-6">
      {{#each formFields}}
      <!-- {{label}} -->
      <div class="form-group">
        <label for="{{name}}" class="block text-sm font-medium text-gray-700 dark:text-gray-300">
          {{label}}{{#if required}} <span class="text-red-500">*</span>{{/if}}
        </label>
        {{#if (eq formControl "textarea")}}
        <textarea
          id="{{name}}"
          data-testid="field-{{name}}"
          formControlName="{{name}}"
          placeholder="{{placeholder}}"
          rows="4"
          class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 dark:bg-gray-800 dark:border-gray-600 dark:text-white sm:text-sm"
          [class.border-red-500]="form.get('{{name}}')?.invalid && form.get('{{name}}')?.touched"
        ></textarea>
        {{else if (eq formControl "checkbox")}}
        <input
          id="{{name}}"
          data-testid="field-{{name}}"
          type="checkbox"
          formControlName="{{name}}"
          class="h-4 w-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-500"
        />
        {{else if (eq formControl "select")}}
        <select
          id="{{name}}"
          data-testid="field-{{name}}"
          formControlName="{{name}}"
          class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 dark:bg-gray-800 dark:border-gray-600 dark:text-white sm:text-sm"
        >
          <option value="">-- Select --</option>
          {{#if enumValues}}
          @for (option of {{name}}Options; track option.value) {
            <option [value]="option.value">\{{ option.label }}</option>
          }
          {{/if}}
        </select>
        {{else}}
        <input
          id="{{name}}"
          data-testid="field-{{name}}"
          type="{{inputType}}"
          formControlName="{{name}}"
          placeholder="{{placeholder}}"
          class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 dark:bg-gray-800 dark:border-gray-600 dark:text-white sm:text-sm"
          [class.border-red-500]="form.get('{{name}}')?.invalid && form.get('{{name}}')?.touched"
          {{#if min}}min="{{min}}"{{/if}}
          {{#if max}}max="{{max}}"{{/if}}
          {{#if maxLength}}maxlength="{{maxLength}}"{{/if}}
        />
        {{/if}}
        @if (form.get('{{name}}')?.invalid && form.get('{{name}}')?.touched) {
          <p class="mt-1 text-sm text-red-600" data-testid="error-{{name}}">
            @if (form.get('{{name}}')?.errors?.['required']) {
              <span data-testid="error-{{name}}-required">{{label}} is required.</span>
            }
            @if (form.get('{{name}}')?.errors?.['minlength']) {
              <span data-testid="error-{{name}}-minlength">Minimum length is \{{ form.get('{{name}}')?.errors?.['minlength'].requiredLength }} characters.</span>
            }
            @if (form.get('{{name}}')?.errors?.['maxlength']) {
              <span data-testid="error-{{name}}-maxlength">Maximum length is \{{ form.get('{{name}}')?.errors?.['maxlength'].requiredLength }} characters.</span>
            }
            @if (form.get('{{name}}')?.errors?.['min']) {
              <span data-testid="error-{{name}}-min">Minimum value is \{{ form.get('{{name}}')?.errors?.['min'].min }}.</span>
            }
            @if (form.get('{{name}}')?.errors?.['max']) {
              <span data-testid="error-{{name}}-max">Maximum value is \{{ form.get('{{name}}')?.errors?.['max'].max }}.</span>
            }
            @if (form.get('{{name}}')?.errors?.['pattern']) {
              <span data-testid="error-{{name}}-pattern">Invalid format.</span>
            }
            @if (form.get('{{name}}')?.errors?.['email']) {
              <span data-testid="error-{{name}}-email">Please enter a valid email address.</span>
            }
          </p>
        }
      </div>
      {{/each}}

      <!-- Form Actions -->
      <div class="flex justify-end gap-3 pt-4 border-t border-gray-200 dark:border-gray-700">
        <button
          type="button"
          (click)="cancelled.emit()"
          data-testid="cancel-button"
          class="rounded-md bg-white px-4 py-2 text-sm font-medium text-gray-700 shadow-sm border border-gray-300 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 dark:hover:bg-gray-700"
        >
          Cancel
        </button>
        <button
          type="submit"
          [disabled]="form.invalid || saving()"
          data-testid="submit-button"
          class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed"
        >
          @if (saving()) {
            <span class="inline-flex items-center">
              <svg class="animate-spin -ml-1 mr-2 h-4 w-4 text-white" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
              </svg>
              Saving...
            </span>
          } @else {
            \{{ mode() === 'create' ? 'Create' : 'Update' }} {{displayName}}
          }
        </button>
      </div>
    </form>
  `,
})
export class {{entityName}}FormComponent {
  private readonly fb = inject(FormBuilder);
  private readonly service = inject({{entityName}}Service);

  // Inputs
  readonly entity = input<{{entityName}} | null>(null);
  readonly mode = input<'create' | 'edit'>('create');

  // Outputs
  readonly saved = output<{{entityName}}>();
  readonly cancelled = output<void>();

  // State
  readonly saving = signal(false);
  readonly error = signal<string | null>(null);

  // Form
  readonly form = this.fb.group({
    {{#each formFields}}
    {{name}}: [{{defaultValue}}, [{{#if required}}Validators.required, {{/if}}{{#if minLength}}Validators.minLength({{minLength}}), {{/if}}{{#if maxLength}}Validators.maxLength({{maxLength}}), {{/if}}{{#if min}}Validators.min({{min}}), {{/if}}{{#if max}}Validators.max({{max}}), {{/if}}{{#if pattern}}Validators.pattern(/{{pattern}}/), {{/if}}{{#if (eq format "email")}}Validators.email, {{/if}}]],
    {{/each}}
  });

  constructor() {
    // Patch form when entity changes
    effect(() => {
      const entity = this.entity();
      if (entity && this.mode() === 'edit') {
        this.form.patchValue(entity as any);
      }
    });
  }

  onSubmit(): void {
    if (this.form.invalid) {
      this.form.markAllAsTouched();
      return;
    }

    this.saving.set(true);
    this.error.set(null);

    const data = this.form.getRawValue();

    const request$ = this.mode() === 'create'
      ? this.service.create(data as {{entityName}}Create)
      : this.service.update(this.entity()!.{{idField}}, data as {{entityName}}Update);

    request$.subscribe({
      next: (result) => {
        this.saving.set(false);
        this.saved.emit(result);
      },
      error: (err) => {
        this.saving.set(false);
        this.error.set(err?.message ?? 'An error occurred');
      },
    });
  }
}

