/**
 * {{entityName}} List Component
 * Generated by @exeris/codegen-ts v0.2.0
 * Angular 21+ with Signals, Resource API, and standalone components
 * DO NOT EDIT - This file is auto-generated
 */

import {
  Component,
  ChangeDetectionStrategy,
  inject,
  signal,
  computed,
  resource,
} from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { FormsModule } from '@angular/forms';
import { {{entityName}}, {{entityName}}Service, PageRequest, {{entityName}}Filter } from '../services/{{kebabCase entityName}}.service';
import { firstValueFrom } from 'rxjs';

@Component({
  selector: 'app-{{kebabCase entityName}}-list',
  standalone: true,
  imports: [CommonModule, RouterModule, FormsModule],
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <div class="space-y-4">
      <!-- Header -->
      <div class="flex items-center justify-between">
        <h1 class="text-2xl font-bold text-gray-900 dark:text-white">{{pluralName}}</h1>
        <a
          routerLink="new"
          data-testid="action-create"
          class="inline-flex items-center rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
        >
          <svg class="-ml-1 mr-2 h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
          </svg>
          New {{displayName}}
        </a>
      </div>

      <!-- Search & Filters -->
      <div class="flex flex-col sm:flex-row gap-4">
        <div class="flex-1">
          <input
            type="search"
            [(ngModel)]="searchQuery"
            (ngModelChange)="onSearch($event)"
            placeholder="Search {{pluralName}}..."
            data-testid="search-input"
            class="block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 dark:bg-gray-800 dark:border-gray-600 dark:text-white sm:text-sm"
          />
        </div>
        {{#each filterableFields}}
        <div class="w-full sm:w-48">
          {{#if (eq formControl "select")}}
          <select
            [(ngModel)]="filter{{pascalCase name}}"
            (ngModelChange)="onFilterChange()"
            data-testid="filter-{{name}}"
            class="block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 dark:bg-gray-800 dark:border-gray-600 dark:text-white sm:text-sm"
          >
            <option value="">All {{label}}</option>
            @for (option of {{name}}Options; track option.value) {
              <option [value]="option.value">\{{ option.label }}</option>
            }
          </select>
          {{/if}}
        </div>
        {{/each}}
      </div>

      <!-- Table -->
      <div class="overflow-hidden shadow ring-1 ring-black ring-opacity-5 rounded-lg">
        @if (dataResource.isLoading()) {
          <div class="flex items-center justify-center py-12">
            <svg class="animate-spin h-8 w-8 text-indigo-600" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
            </svg>
          </div>
        } @else if (dataResource.error()) {
          <div class="text-center py-12">
            <p class="text-red-600 dark:text-red-400">Failed to load data</p>
            <button
              (click)="dataResource.reload()"
              class="mt-2 text-indigo-600 hover:text-indigo-500"
            >
              Retry
            </button>
          </div>
        } @else {
          <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700" data-testid="data-table">
            <thead class="bg-gray-50 dark:bg-gray-800">
              <tr>
                {{#each listColumns}}
                <th
                  scope="col"
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider cursor-pointer hover:bg-gray-100 dark:hover:bg-gray-700"
                  (click)="onSort('{{name}}')"
                >
                  <div class="flex items-center gap-1">
                    {{label}}
                    @if (sortField() === '{{name}}') {
                      @if (sortDirection() === 'asc') {
                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7" />
                        </svg>
                      } @else {
                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                        </svg>
                      }
                    }
                  </div>
                </th>
                {{/each}}
                <th scope="col" class="relative px-6 py-3">
                  <span class="sr-only">Actions</span>
                </th>
              </tr>
            </thead>
            <tbody class="bg-white dark:bg-gray-900 divide-y divide-gray-200 dark:divide-gray-800">
              @for (item of items(); track item.{{idField}}) {
                <tr class="hover:bg-gray-50 dark:hover:bg-gray-800" [attr.data-testid]="'row-' + item.{{idField}}">
                  {{#each listColumns}}
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 dark:text-gray-100">
                    {{#if (eq type "boolean")}}
                    @if (item.{{name}}) {
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">Yes</span>
                    } @else {
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">No</span>
                    }
                    {{else if (eq format "date")}}
                    \{{ item.{{name}} | date:'mediumDate' }}
                    {{else if (eq format "datetime")}}
                    \{{ item.{{name}} | date:'medium' }}
                    {{else}}
                    \{{ item.{{name}} }}
                    {{/if}}
                  </td>
                  {{/each}}
                  <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                    <div class="flex justify-end gap-2">
                      <a
                        [routerLink]="[item.{{idField}}]"
                        [attr.data-testid]="'action-view-' + item.{{idField}}"
                        class="text-indigo-600 hover:text-indigo-900 dark:text-indigo-400 dark:hover:text-indigo-300"
                      >
                        View
                      </a>
                      <a
                        [routerLink]="[item.{{idField}}, 'edit']"
                        [attr.data-testid]="'action-edit-' + item.{{idField}}"
                        class="text-indigo-600 hover:text-indigo-900 dark:text-indigo-400 dark:hover:text-indigo-300"
                      >
                        Edit
                      </a>
                      <button
                        (click)="onDelete(item)"
                        [attr.data-testid]="'action-delete-' + item.{{idField}}"
                        class="text-red-600 hover:text-red-900 dark:text-red-400 dark:hover:text-red-300"
                      >
                        Delete
                      </button>
                    </div>
                  </td>
                </tr>
              } @empty {
                <tr>
                  <td colspan="{{listColumnsCount}}" class="px-6 py-12 text-center text-gray-500 dark:text-gray-400">
                    No {{pluralName}} found
                  </td>
                </tr>
              }
            </tbody>
          </table>
        }
      </div>

      <!-- Pagination -->
      @if (totalPages() > 1) {
        <div class="flex items-center justify-between border-t border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-900 px-4 py-3 sm:px-6">
          <div class="flex flex-1 justify-between sm:hidden">
            <button
              (click)="onPageChange(currentPage() - 1)"
              [disabled]="currentPage() === 0"
              data-testid="pagination-prev-mobile"
              class="relative inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 disabled:opacity-50"
            >
              Previous
            </button>
            <button
              (click)="onPageChange(currentPage() + 1)"
              [disabled]="currentPage() >= totalPages() - 1"
              data-testid="pagination-next-mobile"
              class="relative ml-3 inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 disabled:opacity-50"
            >
              Next
            </button>
          </div>
          <div class="hidden sm:flex sm:flex-1 sm:items-center sm:justify-between">
            <div>
              <p class="text-sm text-gray-700 dark:text-gray-300">
                Showing <span class="font-medium">\{{ (currentPage() * pageSize()) + 1 }}</span>
                to <span class="font-medium">\{{ Math.min((currentPage() + 1) * pageSize(), totalElements()) }}</span>
                of <span class="font-medium">\{{ totalElements() }}</span> results
              </p>
            </div>
            <div>
              <nav class="isolate inline-flex -space-x-px rounded-md shadow-sm" aria-label="Pagination">
                <button
                  (click)="onPageChange(currentPage() - 1)"
                  [disabled]="currentPage() === 0"
                  data-testid="pagination-prev"
                  class="relative inline-flex items-center rounded-l-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0 disabled:opacity-50"
                >
                  <span class="sr-only">Previous</span>
                  <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M12.79 5.23a.75.75 0 01-.02 1.06L8.832 10l3.938 3.71a.75.75 0 11-1.04 1.08l-4.5-4.25a.75.75 0 010-1.08l4.5-4.25a.75.75 0 011.06.02z" clip-rule="evenodd" />
                  </svg>
                </button>
                @for (page of visiblePages(); track page) {
                  <button
                    (click)="onPageChange(page)"
                    [class.bg-indigo-600]="page === currentPage()"
                    [class.text-white]="page === currentPage()"
                    [attr.data-testid]="'pagination-page-' + (page + 1)"
                    class="relative inline-flex items-center px-4 py-2 text-sm font-semibold ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0"
                  >
                    \{{ page + 1 }}
                  </button>
                }
                <button
                  (click)="onPageChange(currentPage() + 1)"
                  [disabled]="currentPage() >= totalPages() - 1"
                  data-testid="pagination-next"
                  class="relative inline-flex items-center rounded-r-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0 disabled:opacity-50"
                >
                  <span class="sr-only">Next</span>
                  <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M7.21 14.77a.75.75 0 01.02-1.06L11.168 10 7.23 6.29a.75.75 0 111.04-1.08l4.5 4.25a.75.75 0 010 1.08l-4.5 4.25a.75.75 0 01-1.06-.02z" clip-rule="evenodd" />
                  </svg>
                </button>
              </nav>
            </div>
          </div>
        </div>
      }
    </div>
  `,
})
export class {{entityName}}ListComponent {
  private readonly service = inject({{entityName}}Service);

  // State
  searchQuery = '';
  {{#each filterableFields}}
  filter{{pascalCase name}} = '';
  {{/each}}

  readonly currentPage = signal(0);
  readonly pageSize = signal(20);
  readonly sortField = signal<string>('{{defaultSortField}}');
  readonly sortDirection = signal<'asc' | 'desc'>('{{defaultSortDirection}}');
  readonly filter = signal<{{entityName}}Filter>({});

  // Resource API for data fetching
  readonly dataResource = resource({
    request: () => ({
      page: this.currentPage(),
      size: this.pageSize(),
      sort: this.sortField(),
      direction: this.sortDirection(),
      filter: this.filter(),
    }),
    loader: async ({ request }) => {
      return firstValueFrom(
        this.service.findAll(
          { page: request.page, size: request.size, sort: request.sort, direction: request.direction },
          request.filter
        )
      );
    },
  });

  // Computed values
  readonly items = computed(() => this.dataResource.value()?.content ?? []);
  readonly totalElements = computed(() => this.dataResource.value()?.totalElements ?? 0);
  readonly totalPages = computed(() => this.dataResource.value()?.totalPages ?? 0);

  readonly visiblePages = computed(() => {
    const total = this.totalPages();
    const current = this.currentPage();
    const pages: number[] = [];

    const start = Math.max(0, current - 2);
    const end = Math.min(total - 1, current + 2);

    for (let i = start; i <= end; i++) {
      pages.push(i);
    }

    return pages;
  });

  protected readonly Math = Math;

  onSearch(query: string): void {
    this.filter.update((f) => ({ ...f, search: query }));
    this.currentPage.set(0);
  }

  onFilterChange(): void {
    this.filter.update((f) => ({
      ...f,
      {{#each filterableFields}}
      {{name}}: this.filter{{pascalCase name}} || undefined,
      {{/each}}
    }));
    this.currentPage.set(0);
  }

  onSort(field: string): void {
    if (this.sortField() === field) {
      this.sortDirection.update((d) => (d === 'asc' ? 'desc' : 'asc'));
    } else {
      this.sortField.set(field);
      this.sortDirection.set('asc');
    }
  }

  onPageChange(page: number): void {
    if (page >= 0 && page < this.totalPages()) {
      this.currentPage.set(page);
    }
  }

  onDelete(item: {{entityName}}): void {
    if (confirm('Are you sure you want to delete this {{displayName}}?')) {
      this.service.delete(item.{{idField}}).subscribe({
        next: () => {
          this.dataResource.reload();
        },
        error: (err) => {
          console.error('Failed to delete:', err);
          alert('Failed to delete {{displayName}}');
        },
      });
    }
  }
}

