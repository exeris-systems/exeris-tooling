package tools.jackson.databind;

import java.io.IOException;

/**
 * Test stub for the Jackson 3.x {@code ObjectMapper}. Generated saga helpers
 * call {@link #readTree(byte[])} to parse the opaque saga context.
 */
public class ObjectMapper {

    public JsonNode readTree(byte[] content) throws IOException {
        return new JsonNode();
    }
}
